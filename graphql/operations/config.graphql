query MobileConfig {
  mobileConfig {
    minSupportedAppVersion
    maxAnalysisSchemaVersion
    supportedCoachLanguages
    jobPollIntervalSeconds
    featureFlags {
      key
      enabled
    }
    currentAiConsentVersion
  }
}
